version: '3.8'

services:
  app:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./backend/data:/app/backend/data
      - ./backend/models:/app/backend/models
    environment:
      - NODE_ENV=production
      - PORT=5000
    restart: unless-stopped
